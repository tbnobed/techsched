from app import is_mobile_device

# Directly test the mobile detection function
print(f"is_mobile_device() returns: {is_mobile_device()}")

# This should print "True" since we modified the function to always return True